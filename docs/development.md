## Getting started

Download and install the client

    $ mkdir {%= name %}
    $ cd {%= name %}
    $ git clone {%= repository.url %} .
    $ git-flow init
    $ npm install
    $ bower install
    $ grunt

And happy coding!
