#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../markdox/bin/markdox" "$@"
  ret=$?
else 
  node  "$basedir/../markdox/bin/markdox" "$@"
  ret=$?
fi
exit $ret
